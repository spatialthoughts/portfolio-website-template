name: portfolio
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - mkdocs-material>=9.5
    # OPTIONAL JUPYTER: Uncomment the line below to use .ipynb files as project pages.
    # Also uncomment the mkdocs-jupyter plugin in mkdocs.yml.
    # - mkdocs-jupyter
